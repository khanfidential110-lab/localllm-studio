# Core dependencies
flask>=2.0.0
huggingface-hub>=0.19.0
requests>=2.25.0

# Inference backends (installed via CI workflow with platform-specific wheels)
# llama-cpp-python>=0.2.0

# Desktop app
pywebview>=4.0
pyinstaller>=6.0

# Optional: for embeddings and additional features
# numpy>=1.20.0
# scipy>=1.9.0
